# RULES.md — 整理ルールと判定

「強いから残す」を自動で決めない。アプリが出すのは **おすすめ** であり、
最終判断は常にユーザーの「残す / 保留 / 整理」。実装は `js/rules.js`。

```text
ポケモン → 客観データ → ルールとの一致 → おすすめ状態 → ユーザーが最終決定
```

---

## 1. 状態

| ID | 表示 | 意味 |
|---|---|---|
| `KEEP` | 🟢 残す | 手元に残す |
| `HOLD` | 🟡 保留 | 判断保留・データ不足 |
| `EXCLUDE` | 🔴 整理 | 博士に送る候補 |

**最終状態** = ユーザー判断があればそれ、なければおすすめ状態。
一覧・検索では「判断済み」と「おすすめのみ」を見分けられるように表示する。

---

## 2. ルール（設定画面のトグル）

初期値は仮設定。すべて設定画面で変更でき、LocalStorage に保存される。

| ID | 分類 | 表示名 | 条件 | 初期値 |
|---|---|---|---|---|
| `raid_top` | レイド | 上位50位以内 | `overall_rank ≤ 50` | ON |
| `raid_type_top` | レイド | タイプ別上位 | `type_rank ≤ 5` | ON |
| `raid_mega` | レイド | メガ・ゲンシ | メガ/ゲンシの評価を通常個体に反映する | ON |
| `raid_shadow` | レイド | シャドウ | シャドウの評価を通常個体に反映する | OFF |
| `pvp_great` | PvP | スーパーリーグ | `rank ≤ 50` | ON |
| `pvp_ultra` | PvP | ハイパーリーグ | `rank ≤ 50` | ON |
| `pvp_master` | PvP | マスターリーグ | `rank ≤ 50` | ON |
| `stat_attack` | 種族値 | 攻撃が高い | `attack ≥ 250` | ON |
| `stat_defense` | 種族値 | 防御が高い | `defense ≥ 230` | OFF |
| `stat_stamina` | 種族値 | HPが高い | `stamina ≥ 230` | OFF |
| `collection` | その他 | コレクション目的 | おすすめを「整理」にしない（保留止まり） | OFF |

しきい値は `js/rules.js` の `THRESHOLDS` で一元管理する。

### 形態の扱い

一覧の1行は `pokemon.md` の1レコード（図鑑番号 + form）。
ランキングは同じ `図鑑番号 + form` のレコードを参照するほか、

- `raid_mega` が ON：同じ図鑑番号の `mega` / `mega-x` / `mega-y` / `primal` の評価を通常個体にも反映
  （メガシンカ元として残す意味があるため）
- `raid_shadow` が ON：同じ図鑑番号の `shadow` の評価（レイド・PvPとも）を通常個体にも反映
  （リトレーンで入れ替える候補として残すため）
- PvPにはメガ/ゲンシは出せないので、`raid_mega` はレイドにだけ効く

反映した場合は詳細画面に「メガで上位」「シャドウで上位」と理由を表示する。

---

## 3. おすすめ判定

1. 有効なルールのうち一致したものを集める
2. 判定の優先度
   - **いずれかの条件を満たす**（初期値）：1つ以上一致 → `KEEP`
   - **すべての条件を満たす**：有効ルールのうち「データが存在するもの」すべてに一致 → `KEEP`
3. `KEEP` でない場合、次のどれかなら `HOLD`
   - レイド総合 100位以内 / タイプ別 10位以内 / 有効なPvPリーグで 100位以内（＝あと一歩）
   - 判定に使えるデータがほぼない（種族値 unknown かつランキングなし）
   - `collection` が ON
4. それ以外は `EXCLUDE`

判定には必ず **理由** を添える（例：「レイド総合 12位」「スーパーリーグ 8位」「攻撃 300」）。

---

## 4. 評価ランク表示（S/A/B/C）

順位から画面表示用のランクを出す。判定そのものには使わない。

| 順位 | ランク |
|---|---|
| 1–10 | S |
| 11–25 | A |
| 26–50 | B |
| 51–100 | C |
| 101– / なし | – |

---

## 5. 検索文字列の生成

Pokémon GO の検索構文：`,`（または `;` `:`）= OR、`&` = AND、`!` = NOT、`1-151` = 範囲。
AND は OR より弱く結合する（`a,b&c` = `(a または b) かつ c`）。

| モード | 生成例 | 使い道 |
|---|---|---|
| 含める（OR） | `150,184,248` | 対象だけを表示 |
| 除外（NOT） | `!150&!184&!248` | 対象**以外**を表示（残すもの以外＝整理候補を出す等） |

- 連続した番号は範囲にまとめる（`1,2,3` → `1-3`）。除外モードでは範囲も `!1-3` で除外できる。
- 追加オプション（どちらのモードでも末尾に `&` で連結。`,` より弱く結合するので全体にかかる）
  - 色違いを除く：`!色違い`
  - 伝説・幻を除く：`!伝説のポケモン&!幻のポケモン`
  - シャドウを除く：`!シャドウ`
- **注意**：図鑑番号は形態を区別できない。同じ番号で「通常は残す・シャドウは整理」のような
  状態の食い違いがある場合、生成画面で警告を出す。
- 検索キーワードの細部（日本語キーワード名など）はゲームの更新で変わりうるため、
  実際にゲーム内で動作確認してから使うこと。
